{
  "name": "melodychain",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npx webpack",
    "preview": "vite preview",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "firebase": "^10.11.1"
  }
}
